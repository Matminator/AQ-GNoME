# Copy this file to .env and fill in your values.
# .env is gitignored and will never be committed.
#
# Usage in a notebook or script:
#   import os
#   api_key = os.environ["MP_API_KEY"]
#
# Set the variable in your shell before launching Jupyter/Python:
#   PowerShell : $env:MP_API_KEY = "your_key"
#   bash/zsh   : export MP_API_KEY="your_key"
# Or add it permanently to your shell profile / IDE environment settings.

MP_API_KEY=your_materials_project_api_key_here
